# 🚀 Инструкция по развертыванию на IONOS

## 📋 Что уже сделано:

✅ Код загружен в GitHub: https://github.com/annbezk777/reports.git
✅ Создан systemd сервис для порта 5500
✅ Включена автосинхронизация (ENABLE_AUTO_SYNC=1)
✅ Создан автоматический скрипт развертывания

---

## 🎯 Что нужно сделать на сервере:

### Вариант 1: Автоматический деплой (рекомендуется)

**На сервере выполните:**

```bash
# 1. Подключиться к серверу
ssh root@69.48.201.233

# 2. Скачать и запустить скрипт развертывания
cd /root
curl -o deploy.sh https://raw.githubusercontent.com/annbezk777/reports/main/deploy_to_ionos.sh
chmod +x deploy.sh
./deploy.sh
```

Скрипт:
- Установит все зависимости
- Склонирует репозиторий
- Создаст виртуальное окружение
- Попросит загрузить google_credentials.json и базу данных
- Настроит systemd сервис
- Запустит приложение на порту 5500

---

### Вариант 2: Ручной деплой

Следуйте инструкции в файле **DEPLOY_CHECKLIST.md**

---

## 📦 Файлы для загрузки на сервер

После запуска скрипта, на **локальной машине** выполните:

### 1. Google Sheets credentials
```bash
scp "/Users/annabereznyak/Desktop/Все проекты/api_google sheets/google_credentials.json" root@69.48.201.233:/root/web-projects/sape-reports/
```

### 2. База данных
```bash
scp "/Users/annabereznyak/Desktop/Все проекты/api_google sheets/database/sape_reports.db" root@69.48.201.233:/root/web-projects/sape-reports/database/
```

---

## ✅ Проверка после развертывания

1. **Открыть в браузере:**
   ```
   http://69.48.201.233:5500/
   ```

2. **Проверить статус сервиса:**
   ```bash
   ssh root@69.48.201.233
   systemctl status sape-reports
   ```

3. **Просмотр логов:**
   ```bash
   journalctl -u sape-reports -f
   ```

4. **Проверить автосинхронизацию:**
   В логах должна быть строка:
   ```
   ✅ Scheduler started: Daily account sync at 10:00 Moscow time
   ```

---

## 🎉 Результат

После успешного развертывания:

- ✅ Панель доступна: **http://69.48.201.233:5500/**
- ✅ Автоматическая синхронизация аккаунтов: **каждый день в 10:00 МСК**
- ✅ Расписание обновления отчетов: **работает!**
- ✅ Автоперезапуск при сбоях
- ✅ Все 66 отчетов и 21 аккаунт доступны

---

## 🔧 Полезные команды

```bash
# Подключение к серверу
ssh root@69.48.201.233

# Статус сервиса
systemctl status sape-reports

# Перезапуск
systemctl restart sape-reports

# Логи в реальном времени
journalctl -u sape-reports -f

# Логи ошибок
tail -f /var/log/sape-reports-error.log

# Обновление кода
cd /root/web-projects/sape-reports
git pull origin main
systemctl restart sape-reports
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверить логи: `journalctl -u sape-reports -n 100`
2. Проверить порт: `netstat -tulpn | grep 5500`
3. Открыть **DEPLOY_CHECKLIST.md** для детальной инструкции

---

**Готово к развертыванию!** 🚀
